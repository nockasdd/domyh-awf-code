---
description: DOMYH Backend Skills for Go/Node/Python API development
globs: src/**/*.go,api/**/*,server/**/*,handlers/**/*,services/**/*,pkg/**/*
alwaysApply: false
---

# Backend Development Skills

## Go Patterns
- Clean Architecture: handlers → services → repositories
- Interface-driven design for testability
- Error handling: `if err != nil { return fmt.Errorf("context: %w", err) }`
- Context propagation for cancellation

## Node.js/Express
- Controller → Service → Repository layers
- Middleware for cross-cutting concerns
- Async/await with proper try-catch
- Input validation with Zod/Joi

## API Design
- RESTful conventions (GET/POST/PUT/DELETE)
- Consistent error responses
- Pagination for lists
- Rate limiting for protection

## Database
- Parameterized queries (SQL injection prevention)
- Connection pooling
- Transactions for atomicity
- Migration scripts for schema changes

## Security
- Input validation on all endpoints
- Authentication middleware
- RBAC for authorization
- Audit logging for sensitive operations
