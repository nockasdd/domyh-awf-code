# =============================================================================
# META.yaml — RAG Patterns
# =============================================================================

name: rag-patterns
version: "6.4.3"
category: ai-ml
priority: 3

desc: "RAG patterns — chunking, embeddings, vector DBs, retrieval optimization, evaluation"

description: |
  Retrieval-Augmented Generation patterns: chunking strategies,
  embedding models, vector databases, retrieval optimization, evaluation.

triggers:
  keywords:
    - rag
    - retrieval augmented
    - embedding
    - vector database
    - chunking
    - semantic search
    - pinecone
    - weaviate
    - qdrant
    - chroma
    - pgvector
    - langchain
    - llamaindex
    - reranking

  intents:
    - "implement RAG pipeline"
    - "setup vector database"
    - "chunking strategy"
    - "embedding model selection"
    - "semantic search"
    - "evaluate RAG quality"

  detect_patterns:
    content: ["VectorStore", "embedding", "chunk", "retriever", "RAG"]

caps:
  - chunking_strategies
  - embedding_selection
  - vector_db_setup
  - retrieval_optimization
  - rag_evaluation
  - hybrid_search

data_files:
  - chunking-strategies.yaml
  - embedding-models.yaml
  - vector-databases.yaml
  - retrieval-patterns.yaml
  - evaluation.yaml

# _DOMYH Awesome Code • RAG Patterns Skill_
