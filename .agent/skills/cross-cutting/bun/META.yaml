name: bun
version: "6.2.5"
category: runtime
tier: 3

desc: "Bun 1.3+ runtime development patterns. Covers all-in-one JS runtime, bundler, test runner, package manager."

patterns:
  total: 24
  domains:
    runtime: 10
    database: 5
    tooling: 9

    - "bun.lockb"
    - "bunfig.toml"
  keywords:
    # Core
    - bun
    - bun runtime
    - bun js
    # Features
    - bun install
    - bun run
    - bun build
    - bun test
    - Bun.serve
    - Bun.file
    - Bun.sql
    # Database
    - bun postgresql
    - bun mysql
    - bun sqlite
    - bun redis
    # Ecosystem
    - elysia
    - hono bun
  intents:
    - create bun project
    - bun api
    - bun server
    - migrate to bun
    - bun database

related_skills:
  - typescript
  - nodejs
  - sql

prevent_confusion_with:
  - nodejs: "Bun is faster, uses bun.lockb"
  - deno: "Bun uses npm, Deno uses URL imports"
