# =============================================================================
# _categories.yaml — Skill Category Definitions
# =============================================================================
# Metadata for skill categories in hierarchical organization
# =============================================================================

version: "1.0.0"

categories:
  # ==========================================================================
  # LANGUAGES (24 skills)
  # ==========================================================================
  languages:
    description: "Programming languages"
    path: "languages/"
    priority: 1
    skills:
      # Systems
      - c
      - cpp
      - rust
      - go
      - zig
      - nim
      - asm

      # JVM
      - java
      - kotlin
      - scala
      - clojure

      # .NET
      - csharp
      - fsharp

      # Scripting
      - python
      - ruby
      - php
      - perl
      - lua
      - javascript
      - typescript

      # Functional
      - haskell
      - elixir
      - ocaml

      # Scientific
      - r
      - julia

      # Blockchain
      - solidity

      # Crystal
      - crystal

  # ==========================================================================
  # FRAMEWORKS (8 skills)
  # ==========================================================================
  frameworks:
    description: "Frontend/Backend frameworks"
    path: "frameworks/"
    priority: 2
    skills:
      - react
      - vue
      - angular
      - svelte
      - nextjs
      - nuxt
      - flutter
      - react-native

  # ==========================================================================
  # DEVOPS (4 skills)
  # ==========================================================================
  devops:
    description: "Infrastructure and deployment"
    path: "devops/"
    priority: 3
    skills:
      - docker
      - kubernetes
      - aws
      - ci-cd

  # ==========================================================================
  # CORE (1 skill - always loaded)
  # ==========================================================================
  core:
    description: "Core security rules - always active"
    path: "core/"
    priority: 0
    always_load: true
    skills:
      - security

  # ==========================================================================
  # CROSS-CUTTING (10 skills)
  # ==========================================================================
  cross-cutting:
    description: "Cross-cutting concerns and utilities"
    path: "cross-cutting/"
    priority: 4
    skills:
      - testing
      - database
      - sql
      - tailwind
      - electron
      - coding-rules
      - ui-ux-pro-max
      - web-perf
      - deno
      - bun

# =============================================================================
# STATISTICS
# =============================================================================

stats:
  total_skills: 51
  categories: 5
  estimated_tokens:
    all_meta: 5100 # ~100 tokens × 51
    router_only: 500 # Router + core META
    typical_session: 2500 # Router + 2 language + 1 framework


# =============================================================================
# _DOMYH Awesome Code v7.0 • Category Definitions_
# =============================================================================
