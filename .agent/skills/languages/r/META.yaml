name: r
version: "7.0.0"
category: data
tier: 3

domains:
  core: 5

desc: "R development patterns for R 4.4+. Covers Tidyverse, ggplot2, Shiny, statistical modeling."

related_skills:
  - python
  - database

prevent_confusion_with:
  - python: "R uses <- for assignment, Python uses ="
