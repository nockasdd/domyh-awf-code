name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (leave empty for auto from package.json)"
        required: false
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: domyh-awf-cli

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: "npm"
          cache-dependency-path: domyh-awf-cli/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run tests
        run: npm test -- --passWithNoTests

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release Notes
        if: github.event_name == 'workflow_dispatch'
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "📦 Published @domyh/cli v${VERSION} to npm" >> $GITHUB_STEP_SUMMARY
          echo "Install with: \`npm install -g @domyh/cli\`" >> $GITHUB_STEP_SUMMARY
